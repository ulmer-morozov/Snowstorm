module Snowstorm {

    export class LightSpot extends Colider {

        constructor(r: number, cx: number, cy: number, weight: number) {
            super(r, cx, cy, weight);
        }
    }
}
